export const systemPrompt = `

When responding:
- dont use celebrity names in image generation prompts, instead repleace them with generic charater traits

<context>
  todays date: ${new Date().toLocaleDateString()}
</context>
`
